# Runtime dependencies for the shared Render instance.
# The deployed service only SERVES a prebuilt Stage1C snapshot. The spatial
# build dependencies (shapely/pyproj, see requirements-stage1b.txt) are not
# reachable from app.stage2c_main and are deliberately not installed here.
# Building Stage1B/1C stays a local step.
fastapi==0.128.2
pydantic==2.13.4
uvicorn==0.48.0
